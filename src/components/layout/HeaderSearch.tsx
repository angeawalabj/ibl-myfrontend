export default function HeaderSearch() {
  return (
    <input
      type="search"
      placeholder="Rechercher un produit, un livre, un service..."
      className="w-full rounded-md border border-border px-4 py-2 text-sm text-text focus:outline-none focus:ring-2 focus:ring-primary"
    />
  );
}
